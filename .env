# Base URL for the kitchen-haven-club Cloudflare Worker. Task 8 added a
# plain POST /api/ai-chat route on this same origin (see MIGRATION_PLAN.md
# Task 8/9). Placeholder until the worker is actually deployed somewhere —
# replace with the real URL then.
EXPO_PUBLIC_API_URL=https://your-worker-domain.example.com
