import { Text, View } from "react-native";

// Web source: kitchen-haven-club/src/routes/app/first-steps/index.tsx
// Stub only — real "Mes premiers pas" UI lands in Task 17.
export default function FirstStepsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-foreground">Mes premiers pas</Text>
    </View>
  );
}
